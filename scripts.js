let sentence = prompt("Enter a Sentence: ");
alert(sentence);

function firstLast() {
  let capitalize = (sentence.charAt(0) + sentence.charAt(sentence.length-1));
  return results = (capitalize.toUpperCase());
  
}
  
alert(firstLast());

function reverseString() {
  let reverse = (sentence.charAt(sentence.length-1) + sentence.charAt(0));
  return reverse.toUpperCase();
}

alert(reverseString());

function conateSentence(firstLast(reverseString() {
});